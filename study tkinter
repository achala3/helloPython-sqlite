#파이썬 import문

from tkinter import *
Tk()

# import tkinter 해서 모듈을 불러오면
  tkinter.Tk()   라고 tkinter 모듈의 Tk()함수라고 알려주어야한다.

* 파이썬의 import는 모듈을 불러오는 명령이고. 불러온모듈의 변수,함수,클래스를 사용할수있게 된다.

    as를 사용하면 

import tkinter as t
t.Tk() 
    
    처럼 tkinter를  쓰기가 귀찮으니 t로 부르겠다해서 t.Tk() 처럼 써도 된다.
    
또한
from math import pi,sqrt  
>>pi
3.141592...
>>sqrt(3,0)
1.71828...
   처럼 math모듈로부터 변수,함수를 콕 찝어 import불러오면 변수함수를 날것 그대로 쓸수있다.
   
 이것도 pi,sqrt,... 나열하기 귀찮으면
 
 from math import *
>>pi
3.141592...
>>sqrt(3,0)
1.71828...

해서 모든(*) 을 쓰면 모두 사용할수 있다.
